import { assert } from "chai";

import { spawn } from "child_process";

describe("killing-methods", () => {
    describe("kill -9 -[PID]", () => {
        it("success killing the apps", async () => {
            let xalwatcher = spawn();
        });
    });
});